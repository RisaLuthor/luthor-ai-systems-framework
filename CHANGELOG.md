# Changelog

## v0.1.0
- FastAPI service scaffold (/health, /evaluate)
- Deterministic policy engine with profile-based rubric
- YAML scenario runner + JSON compliance reports
- Docker image support + CI build/smoke test
